# NEXT.md — Current Sprint Target

> **For agents:** Read this file only. Do not read ROADMAP.md unless you need context on items
> beyond the current PR. Everything you need to start work is here.
>
> **For humans:** Update this file when a PR ships. Move the completed item to ROADMAP.md ✅ table,
> promote the next item from the queue below, and rewrite the "Current PR" block.

> **Bundling guidance — for agents writing code:** When working on the Current PR, if you
> spot adjacent items in the Queue (or in `ROADMAP.md`) that share files, infrastructure,
> or a natural review boundary with the in-flight scope, **flag them as bundling candidates
> in your PR description** rather than expanding the PR mid-flight. Good bundling signals:
> (1) the items touch the same module / shared abstraction, so reviewing them together
> reduces churn (e.g. CAP-004 + MET-001 share `<TrendChart>`); (2) one item validates
> another end-to-end (e.g. a CI guard validates the convention it documents);
> (3) both are S/XS effort and skipping a hand-off cycle saves more than it costs in
> review surface (e.g. AUTO-017.3 + PROC-001 in slot 2). **Bad** bundling signals: items
> in different phases, items that grow the PR past M effort, items that change the
> reviewer's mental model (UX rewrite + backend rewrite), or items the agent identifies
> *after* CI is already green on the original scope. When in doubt, surface the candidate
> bundle as a comment on the PR and let the human decide — never silently expand scope
> beyond the Current PR's `### PR checklist`. Recording the rejected candidates is also
> useful: it builds the dataset for future planning.

---

## ▶ Current PR — AUTO-010 — Root cause analysis and failure clustering
**Effort:** L | **Priority:** 🟢 Differentiator | **Dependencies:** none (composes naturally with AUTO-004 ✅ PR #18 — impact-scoped runs that still fail are the most useful input to clustering, since the noise floor is already lower) | **Source:** `ROADMAP.md` Phase 4 (AUTO-010) — promoted per `NEXT.md` rotation after CAP-002 shipped in PR #3

When 15 tests fail in a run, they often share a single root cause (login endpoint down, auth-service degraded, shared API 5xx). Sentri reports each failure independently — `defectBreakdown` in `Dashboard.jsx` buckets by error-type category but never clusters by **shared cause**. An autonomous QA system should group failures by shared error-message fingerprint, common `sourceUrl`, and common failing step selector, then report "1 root cause → 15 affected tests" so triage stops chasing 15 tickets for one outage. Pairs naturally with AUTO-004's impact-scoped runs (lower noise floor → cleaner clusters) and AUTO-001's risk scorer (high-risk tests are the cluster anchors).

**Files:** new `backend/src/pipeline/failureClusterer.js` (pure function — error-fingerprint hashing + URL-prefix grouping + selector-similarity edit distance, no DB access) · `backend/src/testRunner.js` (call clusterer on run completion, attach `rootCauses` to run record) · `backend/src/database/repositories/runRepo.js` (`rootCauses` JSON field + INSERT_COL) · new migration `027_run_root_causes.sql` · `frontend/src/pages/RunDetail.jsx` (Root Cause Summary panel above the test list) · `frontend/src/api.js` (no new endpoint — `rootCauses` rides on `GET /runs/:runId`) · new `backend/tests/failure-clusterer.test.js`

**Acceptance criteria:**
- A run with 10 failures sharing the same `Error: ECONNREFUSED https://api.example.com/auth` message clusters into a single "likely root cause: auth service unreachable" row with 10 affected tests.
- Failures with truly distinct causes (different URLs, different errors) produce N separate clusters of size 1 each — no false grouping.
- Clustering runs in <100ms for a 100-test run; no LLM calls (deterministic fingerprint hashing only, AI-generated explanations are AUTO-021's scope).
- Runs with zero failures persist `rootCauses: []` and render unchanged (zero regression).
- The panel collapses by default when there's only one cluster, expands automatically when ≥2 clusters surface.

### PR checklist (AUTO-010)

- [ ] New migration `027_run_root_causes.sql` adds `rootCauses` JSON column to `runs`; registered in `runRepo.JSON_FIELDS` + `INSERT_COLS`
- [ ] New pure helper `backend/src/pipeline/failureClusterer.js` exports `clusterFailures({ results })` returning `[{ fingerprint, affectedTestIds[], sharedUrl, sharedSelector, errorPattern, size }]` — no DB access, no LLM calls
- [ ] `backend/src/testRunner.js` calls `clusterFailures` on run completion and persists `run.rootCauses` via `runRepo.update`
- [ ] `frontend/src/pages/RunDetail.jsx` Root Cause Summary panel renders above the test list when `run.rootCauses.length >= 1`; collapses by default for single-cluster, auto-expands for ≥2 clusters
- [ ] `backend/tests/failure-clusterer.test.js` (registered in `run-tests.js`) covers: same-message → single cluster, distinct messages → N singleton clusters, URL-prefix grouping, selector-similarity threshold, zero-failures path, 100ms perf budget on a 100-test fixture
- [ ] `docs/api/projects.md` documents `run.rootCauses[]` shape on `GET /api/v1/runs/:runId`
- [ ] `docs/changelog.md` updated under `## [Unreleased]`
- [ ] `QA.md` § Run Detail extended with root-cause-clustering manual checks (when a panel renders, when it collapses, multi-cluster expansion)
- [ ] `tests/e2e/specs/run-detail-root-cause-ui.spec.mjs` (Tier-3, `page.route()` mock) asserts the panel renders with synthetic `rootCauses` payload

---

<!-- BEGIN COLLAPSED CAP-002 CURRENT-PR BLOCK — preserved for git-history continuity; the canonical record of what shipped lives in the "Recently completed" table below and the `ROADMAP.md` § CAP-002 row.
## ▶ Current PR — CAP-002 — Distributed test sharding across runners
**Effort:** XL (originally L — re-graded after mid-flight investigation of PR #3 `distributed-tests` surfaced four storage-layer prerequisites that the original brief did not call out — see "Prerequisites discovered mid-flight" below) | **Priority:** 🟢 Differentiator | **Dependencies:** INF-002 ✅ (Redis pub/sub for coordinator→shard cancel signal), INF-003 ✅ (BullMQ worker pool primitive) | **Source:** `ROADMAP.md` Phase 4 (CAP-002) — promoted per `NEXT.md` rotation after DIF-012 shipped in PR #2
Single-host parallelism caps suite size at the local worker count (~4–8 on a Render box). Industry tools split a single run across N runners (Cypress Cloud `--record --parallel`, Playwright `--shard=1/4`). Sentri's BullMQ infra already gives us the worker pool primitive, but `runTests()` allocates the entire test list to a single worker, so adding nodes doesn't reduce wall-clock time on large suites. Split `runTests()` into coordinator + N shard workers; coordinator partitions the approved-test list across `runConfig.shards` BullMQ jobs scoped to `(runId, shardIndex, shardCount)`; workers pick their slice, execute, write per-test results back to the shared `runs` row keyed on `runId`; run completes when all shards report, fails if any shard's worker crashes. Re-uses INF-003's abort path — aborting the parent job propagates a cancel signal to all shard jobs via a Redis pub/sub channel.

### Phase 1 — In-process plumbing (PR #3 `distributed-tests`, **open — do not merge until Phase 2 lands on the same branch**)
Per the AGENT.md "no partial PRs" rule, **PR #3 is held open and Phase 2 lands on top of the same branch** before merge — `docs/changelog.md` then reads as one CAP-002 entry, not two. PR #3 already carries:
- `runs.shardCount` + `runs.shardsCompleted` (migration `025_run_shards.sql`) on `INSERT_COLS` + `LEAN_COLS`.
- `shards` request plumbing on `/run` + `/trigger`, server-clamped to `[1, MAX_WORKERS]`, decoupled from `dialsConfig.parallelWorkers` (BUG-0001 — `shardCount > 1` only when the caller explicitly passed `shards`).
- `partitionTestsIntoShards(tests, shardCount)` exported from `backend/src/testRunner.js` — pure Playwright `--shard=N/M` algorithm (first `total % N` shards get +1), tags each test with `_shardIndex`, returns `sizes[]`. **This is the single source of truth Phase 2's coordinator reuses for the split — do not re-implement it in the queue layer.**
- `processResult` advances `run.shardsCompleted` per-shard as each shard's last test reports — the UI badge progresses 0 → N, not 0 → N at completion.
- `RunRegressionModal` `Shards` input (a11y-labelled, integer-coerced) + `RunDetail` blue `Shards M/N` badge.
- `backend/tests/run-sharding.test.js` covering partition contract + route clamp + BUG-0001 + non-numeric fallback.
- `tests/e2e/specs/run-sharding-ui.spec.mjs` covering the modal input + the RunDetail badge presence/absence.

**Files:** `backend/src/testRunner.js` (coordinator splits the test queue into shards, enqueues N BullMQ jobs) · `backend/src/workers/runWorker.js` (accept `shardIndex` / `shardCount`, run only the assigned slice) · `backend/src/routes/runs.js` (accept optional `shards: number` default 1, max bounded by `MAX_WORKERS`) · new migration — add `shardCount`, `shardsCompleted` columns to `runs` (registered in `runRepo.INSERT_COLS`) · `backend/src/utils/redisClient.js` (pub/sub channel for shard coordination + abort propagation) · `frontend/src/components/run/RunRegressionModal.jsx` (shards selector 1-N) · `frontend/src/pages/RunDetail.jsx` (per-shard progress chip) · new `backend/tests/run-sharding.test.js`

### Phase 2 — Cross-process coordinator + storage primitives (**this is what's missing — Codex to implement on the same branch**)

The PR #3 investigation surfaced that the original `NEXT.md` brief assumed a storage layer that doesn't yet support concurrent writers. Phase 2 must land the six prerequisites below **before** wiring the BullMQ fan-out, otherwise concurrent shard workers will silently corrupt run state (last-write-wins on `runRepo.save(run)`, trace zip collision, sibling-shard results wiped on retry).

#### Prerequisites discovered mid-flight (must land in Phase 2 — no skipping, no partial coverage)

**1. Atomic results-append primitive.** `backend/src/database/repositories/runRepo.js:549` `save(run)` writes every column at once from a JS snapshot. N concurrent shard workers calling `save(run)` lose results to last-write-wins. Add `appendRunResults(runId, newResults[])` + `incrementShardsCompleted(runId)` repo methods that append/increment in a single SQL statement (no read-modify-write). Must work on **both SQLite and Postgres** — the Postgres adapter at `backend/src/database/adapters/postgres-adapter.js` does not currently translate `json_extract` / `json_patch`, so the new SQL needs a portable approach (candidate: SQLite `json(? || substr(results, 2))` + parallel Postgres `jsonb_set` / `||`). Existing single-process `runRepo.save(run)` callsites in `testRunner.js` migrate to the new primitive on shard-mode runs only — `shardCount === 1` keeps using `save(run)` for zero-regression. **Required test coverage:** `backend/tests/run-storage-concurrency.test.js` spawning N parallel `appendRunResults` calls and asserting the final array length equals the sum of inputs (no lost writes), on **both** SQLite and Postgres (gate Postgres assertions behind `POSTGRES_URL` like `backend/tests/postgres-adapter.test.js`).

**2. Per-shard trace artifact layout.** `backend/src/testRunner.js:190` writes traces to `${TRACES_DIR}/${runId}.zip` — a single path. N shard workers writing the same path produces a corrupt zip (or the last writer wins, silently dropping the other shards' traces). Switch to `${TRACES_DIR}/${runId}/shard-${shardIndex}.zip` in shard mode; `RunDetail.jsx`'s "Open Trace" action grows a per-shard dropdown when `shardCount > 1` (single-shard runs render the existing single-link UI unchanged). `writeArtifactBuffer` + `signArtifactUrl` continue to work per-shard — no merger required. **Required test coverage:** assertion in `run-sharding.test.js` that a `shardCount: 4` completed run has exactly 4 trace artifact paths persisted; `RunDetail` E2E asserts the per-shard dropdown renders.

**3. Shard-aware retry semantics.** `backend/src/workers/runWorker.js:263` `run.results = (run.results || []).filter(isNonExecutedSkip)` wipes the entire results array on non-final retry. With shards, a single shard's BullMQ retry would erase results from sibling shards that already finished. Change the reset to be shard-scoped: filter out results where `_shardIndex === thisShardIndex` (plus the existing non-executed-skip preservation), and reset `passed`/`failed` from `run.results.length` after the filter rather than to `0`. Pre-shard runs (no `_shardIndex` stamp) keep the existing behaviour. **Required test coverage:** `backend/tests/abort-worker.test.js` extended (or new sibling test) — enqueue 4 shards, simulate shard-2 failure + retry, assert shards 0/1/3's results survive the retry.

**4. Parent-vs-shard run model + concurrent-run guard.** `backend/src/database/repositories/runRepo.js:564` `findActiveByProjectId` returns the existing `runs` row with `status: 'running'`. When the coordinator enqueues N shard jobs sharing the parent `runId`, the guard correctly blocks a second concurrent run — but `POST /runs/:runId/abort` (`backend/src/routes/runs.js:473`) only signals **one** in-memory `workerAbortControllers` entry. Define the contract explicitly: **one `runs` row per parent run**; shards never get their own `runs` row; BullMQ jobs use `jobId: ${runId}:s${shardIndex}` to stay unique while sharing the parent state. The abort route publishes to the new Redis pub/sub channel (prerequisite #5) so every shard worker in every replica receives the cancel signal — `workerAbortControllers` stays as the local fast-path for same-process aborts.

**5. Redis pub/sub abort channel** (the original brief's "shard coordination" channel, now scoped explicitly). New `sentri:run-abort` channel in `backend/src/utils/redisClient.js`. Every shard worker subscribes once at boot via `redisSub.subscribe('sentri:run-abort')` and maintains a `Map<runId, AbortController>` keyed by the runs the worker is currently processing. The abort route publishes `{ runId }` and calls the existing in-memory `workerAbortControllers.abort()` for fast-path same-process aborts; cross-process workers learn via the channel within one Redis round-trip. **Self-echo suppression:** publisher stamps an `origin` id (uuid generated at worker boot, same pattern as `routes/sse.js`'s SSE pub/sub) and subscribers drop messages whose origin matches their own. Degrades cleanly to local-only abort when `REDIS_URL` is unset (existing in-process path is the fallback). **Required test coverage:** new `backend/tests/run-abort-pubsub.test.js` — spawn two subscribers + one publisher against a real Redis (gate behind `REDIS_URL` like `compat-config-cache.test.js`), assert both subscribers receive the message and the publisher self-suppresses.

**6. Shard-crash → run failed.** Extend `runWorker.js` `worker.on('failed', ...)` so a non-recoverable shard-job failure marks the parent `runs` row `status: 'failed'` with `shardsCompleted < shardCount` surfaced (first-writer-wins via an `UPDATE … WHERE status = 'running'` predicate so a second crashed shard doesn't overwrite the first failure reason), then publishes to `sentri:run-abort` so sibling shards drain and exit cleanly. **Required test coverage:** integration test that kills a shard mid-execution (BullMQ `job.moveToFailed()`) and asserts (a) the run row reaches `status: 'failed'`, (b) `shardsCompleted < shardCount`, (c) sibling shards abort within 2s, (d) no orphan BullMQ jobs remain `active` for the dead runId.

**Acceptance criteria (Phase 1 + Phase 2 together — the original brief's criteria, now verifiable end-to-end):**
- `shards: 4` on a 40-test suite splits into 4 BullMQ jobs of ~10 tests each, completing in **~1/4 the wall-clock time** of a single-shard run. Verified by a new `tests/e2e/specs/run-sharding-wallclock.spec.mjs` Tier-1 spec that times both runs against a real Playwright harness (gate behind `RUN_E2E_REAL_PLAYWRIGHT=true` so CI matrix can opt in once the harness exists; otherwise asserts shard count + completion only).
- `shards: 1` (default) behaves identically to today — zero regression for the existing JWT + trigger-token paths. Verified by the existing `backend/tests/run-tests.js` suite passing unchanged under `shardCount === 1`.
- Aborting the parent run via `POST /api/v1/runs/:runId/abort` propagates to all shard workers via Redis pub/sub; orphaned shard jobs are not possible. Verified by prerequisite #5's test.
- A shard worker crashing mid-execution marks the run `failed` with `shardsCompleted < shardCount` surfaced in the response, not silently completes. Verified by prerequisite #6's test.
- `MAX_WORKERS` clamp prevents `shards: 100` from exhausting the worker pool; clamped server-side, never client-trusted. Verified by `run-sharding.test.js` (already in PR #3).
- Concurrent shard writes to the same run row never lose results — verified by prerequisite #1's concurrency test on both SQLite and Postgres.
- Per-shard trace zips render in the `RunDetail` trace dropdown for `shardCount > 1` runs and continue rendering as a single link for `shardCount === 1` runs.

### PR checklist (CAP-002 — Phases 1 + 2 combined, single merge)

**Phase 1 (PR #3, already on the branch — do not redo):**
- [x] Migration `025_run_shards.sql` adds `shardCount` + `shardsCompleted` to `runs` (registered in `runRepo.INSERT_COLS` + `LEAN_COLS`)
- [x] `backend/src/routes/runs.js` + `backend/src/routes/trigger.js` accept optional `shards: number` (default 1, server-clamped to `MAX_WORKERS`); decoupled from `dialsConfig.parallelWorkers` (BUG-0001)
- [x] `partitionTestsIntoShards()` exported from `backend/src/testRunner.js` — pure Playwright `--shard=N/M` algorithm; tags each test with `_shardIndex`; returns `sizes[]`
- [x] `processResult` advances `run.shardsCompleted` per-shard as each shard's last test reports
- [x] `frontend/src/components/run/RunRegressionModal.jsx` `Shards` input (a11y-labelled, integer-coerced); `frontend/src/pages/RunDetail.jsx` blue `Shards M/N` badge when `shardCount > 1`
- [x] `backend/tests/run-sharding.test.js` (registered in `run-tests.js`) covers partition contract + route clamp + BUG-0001 + non-numeric fallback
- [x] `tests/e2e/specs/run-sharding-ui.spec.mjs` covers the modal input + the RunDetail badge presence/absence

**Phase 2 (Codex to implement on the same branch — must land before merge):**
- [x] **Prerequisite #1** — `runRepo.appendRunResults(runId, newResults[])` + `runRepo.incrementShardsCompleted(runId)` atomic primitives, dual-dialect SQL for SQLite + Postgres (cross-dialect via raw-JSON-TEXT `substr`/`||` splice — avoids SQLite-only `json_patch` and Postgres-only `jsonb_set` since `postgres-adapter.js` doesn't bridge either); `backend/tests/run-storage-concurrency.test.js` proves no lost writes under 8× concurrent `appendRunResults` and `incrementShardsCompleted` calls on whichever dialect `DATABASE_URL` selects
- [x] **Prerequisite #2** — per-shard trace paths `${TRACES_DIR}/${runId}/shard-${shardIndex}.zip` via new pure `shardTraceArtifactPath(runId, shardIndex)` helper (single source of truth, exported for test-side contract verification). Migration `026_run_trace_paths.sql` adds nullable `runs.tracePaths` JSON column (registered in `runRepo.JSON_FIELDS` + `INSERT_COLS`); the runner populates it sparsely in shard mode and leaves it NULL for single-shard runs so `run.tracePath` stays bit-for-bit identical for legacy consumers. `signRunArtifacts` signs every `tracePaths[]` entry alongside `tracePath`. `RunDetail.jsx` renders a `<select>` dropdown when `tracePaths.length > 1`; otherwise falls back to the existing single-link UI. Coverage in `backend/tests/run-sharding.test.js` asserts (a) `shardIndex == null` → legacy URL (zero regression), (b) shard-mode path is nested + indexed, (c) a `shardCount: 4` run produces exactly 4 distinct trace paths.
- [x] **Prerequisite #3** — shard-scoped retry reset in `backend/src/workers/runWorker.js` (filter survivors by `_shardIndex !== thisShardIndex` plus the existing non-executed-skip preservation; re-derive `passed`/`failed` from survivors rather than resetting to 0). `processResult` in `testRunner.js` now stamps `_shardIndex` onto every persisted result row so the worker has the attribution it needs across a DB round-trip. `shardIndex` is threaded from `job.data` with a `null` default so legacy single-process runs go through the prior wipe-all path bit-for-bit (zero regression). Coverage: `backend/tests/run-worker-shard-retry.test.js` exercises legacy wipe-all, sibling-shard preservation, non-executed-skip survival, passed/failed re-derivation, and pre-Phase-2 (no `_shardIndex` stamp) legacy-row safety.
- [x] **Prerequisite #4** — parent-vs-shard model defined explicitly: **one `runs` row per parent run**, shards never get their own row, BullMQ `jobId: ${runId}:s${shardIndex}` keeps each shard job unique while sharing parent state via the row. `workerAbortControllers` now keys by jobId (legacy single-shard runs: bare `runId`; shard-mode runs: `${runId}:s${i}`) and stores `runId` on the entry so reverse lookups by parent run work. Three new helpers in `backend/src/workers/runWorker.js` capture the parent/shard pattern: `workerAbortKey(runId, shardIndex)`, `forEachShardEntry(runId, fn)`, and `abortAllShardsForRun(runId)` (snapshot-based iteration so handlers can safely delete during the loop). The abort route's local-fast-path and the Prerequisite #5 pub/sub subscriber both fan out via `abortAllShardsForRun`, so cross-replica aborts hit every shard's controller — the local fast-path stays as the same-process accelerator. Legacy single-shard runs register at the bare-runId key and keep their existing single-`get`/`delete` semantics bit-for-bit (zero regression — Phase 1 callers never pass `shardIndex`, so `workerAbortKey(runId, null)` returns the runId unchanged). Coverage: `backend/tests/run-shard-registry.test.js` exercises key derivation, no-collision multi-shard registration, parent-keyed iteration with concurrent mutation, sibling-run isolation under fan-out abort, and unknown-runId no-throw safety. **Deferred to the coordinator PR**: the actual queue-producer-side `jobId: ${runId}:s${i}` enqueue (the coordinator hasn't been built yet) — the worker side is ready to receive shard-keyed jobs the moment the coordinator fans them out.
- [x] **Prerequisite #5** — `sentri:run-abort` Redis pub/sub channel encapsulated in a new `backend/src/utils/runAbortChannel.js` module (kept separate from `redisClient.js` so the channel name + origin-stamp protocol are testable in isolation and reusable by Prerequisite #6). Exports `publishRunAbort(runId)` + `subscribeToRunAborts({ onAbort })`; per-process `RUN_ABORT_ORIGIN` (16-byte hex, same shape as `routes/sse.js:_instanceId`) suppresses self-echo. The runWorker subscribes at boot inside `startWorker()`; the handler maps the runId back to the local `workerAbortControllers` map and aborts in the same way the in-process abort route does — so cross-replica aborts and same-replica aborts share one code path. The abort route in `backend/src/routes/runs.js` publishes after the local-fast-path (best-effort, fire-and-forget). Degrades cleanly to local-only abort when `REDIS_URL` is unset (`isRedisAvailable() → false` makes both functions no-ops). Coverage: `backend/tests/run-abort-pubsub.test.js` asserts (a) sibling-origin publish invokes `onAbort` exactly once, (b) self-origin publish does NOT invoke `onAbort`, (c) malformed payloads (invalid JSON / missing runId / non-string runId) are silently ignored and don't poison the subscriber, (d) `subscribeToRunAborts` validates its handler, (e) `publishRunAbort` returns false on bad input without throwing. Tests (a)–(c) gate on `REDIS_URL` and skip cleanly otherwise — mirrors `backend/tests/compat-config-cache.test.js`'s cross-process pattern.
- [x] **Prerequisite #6** — `runWorker.js` final-attempt failure path now uses the new `runRepo.markRunFailedFirstWriterWins(runId, { error, errorCategory })` atomic UPDATE for shard-mode runs (predicate `WHERE id = ? AND status = 'running'` — second-place shards become clean no-ops, preserving the first crasher's classified error in the audit trail). After persisting the terminal state the worker publishes to `sentri:run-abort` via the Prerequisite #5 channel so sibling shards in *other* replicas receive the cancel signal and drain (same channel as the user-driven abort route — one cancellation pathway for both manual and crash-driven aborts). `shardsCompleted` is *deliberately* not flushed so the badge surfaces a truthful partial completion (`< shardCount`). Activity logs + SSE `done` events fire only for the first writer to prevent duplicate notifications. Legacy single-shard runs (`shardIndex == null`) keep the prior `runRepo.save(run)` path bit-for-bit. Coverage: `backend/tests/run-shard-crash.test.js` exercises (1) first-call writes + returns true, (2) second-call is no-op + first writer's error survives, (3) `shardsCompleted < shardCount` preserved, (4) 4 concurrent crashes land exactly one writer, (5)+(6) already-terminal (aborted / completed) rows are not flipped by late crashes, (7) missing / invalid runIds return false without throwing. **Deferred to the coordinator PR**: the end-to-end BullMQ-kill integration test (NEXT.md (a)–(d) acceptance) — needs a real Redis + worker harness to call `job.moveToFailed()` and observe sibling-shard drain. The first-writer-wins primitive verified here is the *contract* every shard worker depends on for that integration check.
- [x] `backend/src/testRunner.js` coordinator partitions the approved-test list into N BullMQ jobs scoped to `(runId, shardIndex, shardCount)` — fan-out lives in `backend/src/routes/runs.js:281-330` + `backend/src/routes/trigger.js:646-720`, reusing the Phase 1 `partitionTestIdsForShards` helper as the single source of truth for the split
- [x] `backend/src/workers/runWorker.js` accepts `shardIndex` / `shardCount` from job data, runs only the assigned slice via the new `test_run_shard` branch; writes via the atomic primitives (`appendRunResults`, `incrementRunStats`, `setShardTracePath`, `purgeShardResults`, `markRunCompletedFirstWriterWins`) not `runRepo.save(run)`. The boundary-crossing shard (whose `incrementShardsCompleted` UPDATE crosses the cap) hands off to the inline `finalizeShardedRun` finalizer
- [ ] `tests/e2e/specs/run-sharding-wallclock.spec.mjs` Tier-1 spec proves `shards: 4` completes a 40-test suite in ~1/4 the single-shard wall-clock time (gate behind `RUN_E2E_REAL_PLAYWRIGHT=true`) — **Deferred to CAP-002b Gap 1 in ROADMAP.md**: spec code lands in the follow-up alongside the real BullMQ + Playwright harness; the harness itself (Redis + 4× worker containers + a deterministic test target) is the work, not the spec. Documented per AGENT.md issue-handling rule
- [x] `docs/api/projects.md` documents the `shards` run-request shape and per-shard trace artifact layout (sparse `tracePaths[]`, abort behaviour, finalization handoff, CI/CD callbackUrl on sharded runs)
- [x] `docs/changelog.md` updated under `## [Unreleased]` as a single CAP-002 entry covering Phases 1 + 2 + coordinator (#7) + atomic primitives (#8) + retry primitive (#9) + late-abort race (#10) + sharded callback (#11) + GitHub Check on sharded runs (#12) — replaces the prior `CAP-002 (partial)` framing
- [x] `QA.md` § Distributed Sharding (CAP-002) added with 24-step manual test plan covering shards selector, per-shard badge progression, per-shard trace dropdown, coordinator fan-out, finalization handoff, race-safety scenarios (concurrent finish, mid-run abort, late-abort race, shard crash, cross-replica abort), CI/CD callback, permissions, and edge cases. Intent-map + section-index entries added.

END COLLAPSED CAP-002 CURRENT-PR BLOCK -->

---

## ⏭ Queue (next 3 PRs after current)
### 1 · DIF-015c (Gaps 2/3/5/6) — Recorder gaps completion
**Effort:** M (bundled 4×S) | **Priority:** 🟢 Differentiator (parity with BearQ / Mabl / Testim) | **Dependencies:** DIF-015 ✅, DIF-015b ✅, DIF-015c Gap 1 ✅ PR #11, DIF-015c Gap 2 backend ✅ PR #118 | **Source:** `ROADMAP.md` Phase 3 (DIF-015c sub-items)

_(promoted from queue slot 2 to slot 1 after AUTO-010 moved to Current PR. Original scope unchanged — see prior NEXT.md revisions for the full Gap 2/3/5/6 breakdown, or the `ROADMAP.md` § DIF-015c sub-section.)_

### 2 · AUTO-008 — Distributed runner across multiple machines
**Effort:** XL | **Priority:** 🟢 Differentiator | **Dependencies:** INF-003 ✅, INF-002 ✅, CAP-002 ✅ (PR #3 — the BullMQ shard-job pattern AUTO-008 generalises to multi-host worker pools) | **Source:** `ROADMAP.md` Phase 4 (AUTO-008)

CAP-002 (PR #3) shipped the cross-process sharding primitives end-to-end on a single replica with a fixed `MAX_WORKERS` pool. AUTO-008 extracts the shard worker into a standalone, stateless container image so the worker pool can scale horizontally across multiple machines: the HTTP server enqueues sharded jobs (already does this as of CAP-002); any available worker container in the pool picks them up via BullMQ's worker-concurrency model. Exposes worker count + queue depth on the dashboard.

**Files:** `backend/src/workers/runWorker.js` (make fully stateless and containerisable — verify no in-memory state survives across job picks) · new `backend/Dockerfile.worker` (worker-only image) · `docker-compose.yml` (add scalable `worker` service alongside `backend`) · `frontend/src/pages/Dashboard.jsx` (worker pool status panel — queue depth + active count + per-worker last-seen)

**Acceptance criteria:**
- 4 worker containers running against the same Redis: a `shards: 4` run lands one shard on each container, completing in ~1/4 the single-worker wall-clock time (the CAP-002b Gap 1 harness becomes the acceptance test for AUTO-008 too).
- Killing a worker container mid-run: in-flight shard's BullMQ job is re-queued (`attempts: 2`), picked up by a surviving worker, the run finalises correctly (no orphan `active` jobs after the dead container's grace period).
- Dashboard worker-pool panel shows: queue depth (`waiting + delayed`), active count, per-worker last-seen heartbeat. Stale workers (last-seen > 60s) render dimmed.

> **Phase 5 audit-hardening blockers** (`SEC-004` MFA, `SEC-006` PII firewall, `INF-007` OTel/Sentry, `INF-008` Postgres-default + dual-DB CI matrix, `AUTO-022` AI eval harness) remain queued in `ROADMAP.md` Phase 5 — interleave with feature delivery per the "Recommended PR order" block at the bottom of ROADMAP.md.

<!-- BEGIN COLLAPSED PRIOR-QUEUE-SLOT-1 (AUTO-010) — moved to Current PR; preserved for git-history continuity.
### 1 · AUTO-010 — Root cause analysis and failure clustering
**Effort:** L | **Priority:** 🟢 Differentiator | **Dependencies:** none (composes naturally with AUTO-004 ✅ PR #18 — impact-scoped runs that still fail are the most useful input to clustering, since the noise floor is already lower) | **Source:** `ROADMAP.md` Phase 4 (AUTO-010)
When 15 tests fail in a run, they often share a single root cause (login endpoint down, auth-service degraded, shared API 5xx). Sentri reports each failure independently — `defectBreakdown` in `Dashboard.jsx` buckets by error-type category but never clusters by **shared cause**. An autonomous QA system should group failures by shared error-message fingerprint, common `sourceUrl`, and common failing step selector, then report "1 root cause → 15 affected tests" so triage stops chasing 15 tickets for one outage. Pairs naturally with AUTO-004's impact-scoped runs (lower noise floor → cleaner clusters) and AUTO-001's risk scorer (high-risk tests are the cluster anchors).
**Files:** new `backend/src/pipeline/failureClusterer.js` (pure function — error-fingerprint hashing + URL-prefix grouping + selector-similarity edit distance, no DB access) · `backend/src/testRunner.js` (call clusterer on run completion, attach `rootCauses` to run record) · `backend/src/database/repositories/runRepo.js` (`rootCauses` JSON field + INSERT_COL) · new migration `023_run_root_causes.sql` · `frontend/src/pages/RunDetail.jsx` (Root Cause Summary panel above the test list) · `frontend/src/api.js` (no new endpoint — `rootCauses` rides on `GET /runs/:runId`) · new `backend/tests/failure-clusterer.test.js`

**Acceptance criteria:**
- A run with 10 failures sharing the same `Error: ECONNREFUSED https://api.example.com/auth` message clusters into a single "likely root cause: auth service unreachable" row with 10 affected tests.
- Failures with truly distinct causes (different URLs, different errors) produce N separate clusters of size 1 each — no false grouping.
- Clustering runs in <100ms for a 100-test run; no LLM calls (deterministic fingerprint hashing only, AI-generated explanations are AUTO-021's scope).
- Runs with zero failures persist `rootCauses: []` and render unchanged (zero regression).
- The panel collapses by default when there's only one cluster, expands automatically when ≥2 clusters surface.
### 2 · DIF-015c (Gaps 2/3/5/6) — Recorder gaps completion
**Effort:** M (bundled 4×S) | **Priority:** 🟢 Differentiator (parity with BearQ / Mabl / Testim) | **Dependencies:** DIF-015 ✅ (recorder), DIF-015b ✅ (`selectorGenerator` at `backend/src/runner/recorder.js:292`), DIF-015c Gap 1 ✅ PR #11 (paste + opt-in keyboard shortcuts), DIF-015c Gap 2 **backend** ✅ PR #118 (`POST /api/v1/projects/:id/record/:sessionId/assertion` + `addAssertionAction()` at `backend/src/runner/recorder.js:827-855` for `assertVisible` / `assertText` / `assertValue` / `assertUrl`) | **Source:** `ROADMAP.md` Phase 3 (DIF-015c sub-items) — closes the recorder backlog **except Gap 4** (auth / `storageState` integration, M effort, depends on DIF-010 multi-auth profiles — defer until DIF-010 ships)

PR #118 shipped recorder action vocabulary + assertion backend, but four gaps remain that competitors all ship as table stakes. Bundling is justified per the preamble — all four touch the same two modules (`backend/src/runner/recorder.js` `RECORDER_SCRIPT` at lines 180-395 + `RecorderModal.jsx` / `LiveBrowserView.jsx`), so one review cycle replaces three. Gap 4 is **excluded** because it depends on the not-yet-shipped DIF-010 multi-auth profiles and would balloon scope past M.

- **Gap 2 (S) — Inline assertion point-and-click UX + 2 new kinds.** Frontend `assertMode` prop on `LiveBrowserView` swaps pointer forwarding for hover-only; CDP `Overlay.highlightNode` traces the hovered element via the existing CDP session reused from the screencast path; click opens an assertion picker pre-filled with that element's `selectorGenerator`-derived selector. Add the two kinds the form already accepts server-side but `actionsToPlaywrightCode` (`backend/src/runner/recorder.js:677-817`) doesn't emit — `assertCount` → `await expect(locator).toHaveCount(N)`, `assertHasClass` → `await expect(locator).toHaveClass(/pattern/)`. Manual-paste form preserved as fallback per AGENT.md "never remove an escape hatch".
- **Gap 3 (S) — Pause / resume / undo.** New `session.paused` flag + `pause()` / `resume()` / `popLastAction()` helpers in `recorder.js`; `forwardInput` (`backend/src/runner/recorder.js:1573`) early-returns when paused; `popLastAction` pulls from the session's captured-actions array with a no-op when empty. New routes `POST /record/:sessionId/pause` + `/resume` + `/pop-last` (qa_lead+, mirror existing `/input` permission shape). RecorderModal toolbar gains the three controls, ARIA-labelled, disabled when no actions yet captured.
- **Gap 5 (S) — Device profile during recording.** Thread a `device` param through `POST /projects/:id/record` and `startRecording()` in `recorder.js`; resolve via `resolveDevice(device)` from `backend/src/runner/config.js` (reuse the DIF-003 helper); pass to `browser.newContext({ ...devices[device] })` the same way `executeTest.js` already does at run-time. RecorderModal reuses the device dropdown from `RunRegressionModal.jsx` — extract to a shared `<DeviceSelector>` if it'll be referenced twice long-term.
- **Gap 6 (S) — Stealth launch profile.** `launchBrowser({ stealth })` opt-in flag in `backend/src/runner/config.js`; new `applyStealth(context)` helper that calls `context.addInitScript()` with `Object.defineProperty(navigator, 'webdriver', { get: () => undefined })` plus a minimal plugin-list shim. **Do NOT add `playwright-extra`** per AGENT.md "no large dependencies". Env var `RECORDER_STEALTH=true` toggles default-on for the recorder path only — runtime test execution unaffected.

**Files (grouped by gap, single PR):**
- Gap 2 — `frontend/src/components/run/LiveBrowserView.jsx` · `frontend/src/components/run/RecorderModal.jsx` · `backend/src/runner/recorder.js` (extend `addAssertionAction` validator + `recordedActionToStepText` at lines 521-616 + `actionsToPlaywrightCode` branches + `isEmittableAction` at lines 634-665) · `backend/src/routes/tests.js` (assertion-kind allowlist 4 → 6)
- Gap 3 — `backend/src/runner/recorder.js` (`pause` / `resume` / `popLastAction` helpers + `forwardInput` guard) · `backend/src/routes/tests.js` (three new routes) · `backend/src/middleware/permissions.json` · `frontend/src/components/run/RecorderModal.jsx` · `frontend/src/api.js` (PROC-001 callsites)
- Gap 5 — `backend/src/runner/recorder.js` `startRecording({ device })` · `backend/src/routes/tests.js` `POST /projects/:id/record` validator · `frontend/src/components/run/RecorderModal.jsx` device dropdown
- Gap 6 — `backend/src/runner/config.js` (`launchBrowser` `stealth` opt-in + `applyStealth` helper) · `backend/src/runner/recorder.js` (`startRecording` reads `RECORDER_STEALTH`) · `backend/.env.example`
- Tests — `backend/tests/recorder.test.js` extends with: assertCount/assertHasClass validator + code emission + step prose; pause-blocks-input + undo-pops-last-action + pop-when-empty no-ops; device passthrough to `newContext`; stealth init-script presence on the recorder context only
- E2E — `tests/e2e/specs/recorder-completion-ui.spec.mjs` (Tier-3, `page.route()` screencast mocks per `tests/e2e/COVERAGE.md`) covers: hover-to-pick assertion round-trip (Gap 2), pause hides next action from the captured list (Gap 3), device selection (Gap 5)

**Acceptance criteria:**
- All 6 assertion kinds round-trip end-to-end via point-and-click (no manual selector paste required); manual-paste form continues to work as fallback.
- Pause → take action → resume → confirm only post-resume actions land in `session.actions`. Undo pops the most recent action; pop-when-empty is a no-op (no 4xx, no crash).
- Recording on iPhone 14 emulation produces `safeClick` / `safeFill` calls that replay against the same device profile in `executeTest.js` (DIF-003 round-trip).
- Stealth-on recorder context: `navigator.webdriver === undefined` at the page level; runtime test execution (`executeTest.js`) is **untouched** by this flag — verify by inspecting `executeTest.js` for zero references to `RECORDER_STEALTH`.
- Viewer attempts to call any of the new routes (`/pause` / `/resume` / `/pop-last`) → 403 (existing `requireRole("qa_lead")` gate on recorder routes unchanged).
- Pre-PR-#118 recordings continue to load and replay correctly — zero regression on existing `session.actions` shapes.

**Bundling rationale (per preamble):** ✓ same module surface (`recorder.js` + `RecorderModal.jsx`), ✓ four × S = M total, ✓ shared reviewer mental model (recorder UX completion), ✓ Gap 4 explicitly excluded so scope doesn't drift. Anti-pattern guard: do NOT bundle Gap 4 in mid-flight — it needs DIF-010's credential-profile primitive that's not in any of the current queue slots.

> **Phase 5 audit-hardening blockers** (`SEC-004` MFA, `SEC-006` PII firewall, `INF-007` OTel/Sentry, `INF-008` Postgres-default + dual-DB CI matrix, `AUTO-022` AI eval harness) remain queued in `ROADMAP.md` Phase 5 — interleave with feature delivery per the "Recommended PR order" block at the bottom of ROADMAP.md.

END COLLAPSED PRIOR-QUEUE-SLOT-1 -->

---

## ✅ Recently completed

| ID | Title | PR |
|----|-------|----|
| CAP-002 | Distributed test sharding across runners. End-to-end cross-process sharding for `POST /api/v1/projects/:id/run` and `POST /api/v1/projects/:id/trigger`. `shards: N > 1` fans the run out across N BullMQ shard workers (one BullMQ job per shard, sharing a parent `runId` via the `${runId}:s${shardIndex}` jobId convention); each shard executes a contiguous slice of the dispatched test queue using the Playwright `--shard=N/M` algorithm (first `total % N` shards receive +1 test). The boundary-crossing shard (whose `incrementShardsCompleted` UPDATE crosses the `shardCount` cap — atomic, row-locked, exactly-once by SQL predicate) is the single finalizer per run: it evaluates quality gates + Web Vitals against the full DB results, runs the AI feedback loop once, transitions the run to `completed` via `markRunCompletedFirstWriterWins` (first-writer-wins UPDATE catches the late-abort race), emits the single `done` SSE event, fires notifications + GitHub Check completion + the optional CI/CD `callbackUrl`. **Phase 1 — Plumbing**: migration `025_run_shards.sql` (`shardCount` + `shardsCompleted` columns) and `026_run_trace_paths.sql` (`tracePaths` JSON column); `shards` request plumbing on `/run` + `/trigger`, server-clamped to `[1, MAX_WORKERS]`, decoupled from `dialsConfig.parallelWorkers` (BUG-0001 — `shardCount > 1` only when caller explicitly passed `shards`); `partitionTestsIntoShards` + `partitionTestIdsForShards` exported from `backend/src/testRunner.js`; `RunRegressionModal` `Shards` input + `RunDetail` blue `Shards M/N` badge + per-shard trace `<select>` dropdown. **Phase 2 — Storage primitives**: 6 new repo helpers in `backend/src/database/repositories/runRepo.js` — `appendRunResults` (atomic JSON splice via `substr` + `||`, dual-dialect SQLite/Postgres), `incrementShardsCompleted` (capped UPDATE with `WHERE shardsCompleted < shardCount` predicate so info.changes accurately reports advancement), `incrementRunStats` (atomic per-shard pass/fail/totalDelta accumulator), `setShardTracePath` (transaction-wrapped sparse-array write with dialect-conditional `FOR UPDATE` on Postgres), `purgeShardResults` (atomic shard-scoped retry purge replacing the lost-write `save(run)` pattern), `markRunFailedFirstWriterWins` + `markRunCompletedFirstWriterWins` (first-writer-wins predicate `WHERE status = 'running'` for crash + late-abort race safety). **Phase 2 — Worker**: new `test_run_shard` branch in `backend/src/workers/runWorker.js` resolves the pre-partitioned slice, calls `runTests` with `shardIndex`, atomically composes deltas via `incrementRunStats`, calls `incrementShardsCompleted` for the finalization handoff. New `workerAbortKey(runId, shardIndex)` + `forEachShardEntry` + `abortAllShardsForRun` parent/shard registry helpers. **Phase 2 — Abort propagation**: new `backend/src/utils/runAbortChannel.js` module encapsulates the `sentri:run-abort` Redis pub/sub channel with per-process `RUN_ABORT_ORIGIN` for self-echo suppression and a separate `_messageHandlerRegistered` flag so SUBSCRIBE retries don't stack duplicate listeners; the abort route fans out via `abortAllShardsForRun` then publishes. **Phase 2 — Trigger callback on sharded runs**: `callbackUrl` rides in every shard's job options; the finalizer fires it exactly once (gated by `incrementShardsCompleted` exclusivity), same payload shape as single-shard runs; SSRF-safe via `safeFetch`. **Phase 2 — GitHub Check completion**: `concludeGithubCheck` exported from `routes/trigger.js`, called from `finalizeShardedRun` via dynamic import (avoids circular dep). **Zero-regression guarantees**: single-shard runs (`shardCount === 1`) and the no-Redis fallback go through `runRepo.save(run)` + `runWithAbort` bit-for-bit; legacy callers see no behaviour change. **Coverage**: 7 dedicated backend test files all registered in `backend/tests/run-tests.js` — `run-sharding` (partition algorithm + route clamp + BUG-0001 + `partitionTestIdsForShards` slice contract), `run-storage-concurrency` (8× concurrent `appendRunResults` no-lost-writes on whichever dialect `DATABASE_URL` selects), `run-shard-finalizer` (exactly-one-finalizer race + stats composition + over-firing + 10-way interleave), `run-shard-crash` (`markRunFailedFirstWriterWins` + `markRunCompletedFirstWriterWins` first-writer-wins + late-abort race + already-terminal preservation), `run-shard-registry` (parent/shard registry + concurrent mutation + sibling-run isolation), `run-worker-shard-retry` (shard-scoped retry reset + sibling-shard preservation + non-executed-skip survival + legacy wipe-all bit-for-bit), `run-abort-pubsub` (cross-replica delivery + self-echo suppression + malformed payload safety, gated on `REDIS_URL`). UI coverage in `tests/e2e/specs/run-sharding-ui.spec.mjs`. **Deferred to ROADMAP.md § CAP-002b** (SaaS-readiness follow-ups, 10 sub-items): wall-clock Tier-1 E2E harness (Gap 1), BullMQ-kill chaos test (Gap 2), route-mock coordinator test (Gap 3), auto-scaling shard workers (Gap 4), DLQ + replay UI (Gap 5), per-tenant fair scheduling (Gap 6), duration-aware shard balancing (Gap 7), cross-region distribution (Gap 8), container-per-shard isolation (Gap 9), Redis HA enforcement (Gap 10). `docs/api/projects.md` § Run sharding documents the request shape, per-shard trace artifact layout, abort behaviour, finalization handoff, and CI/CD callback semantics; `docs/changelog.md` updated under `## [Unreleased]` as a single CAP-002 entry covering Phases 1 + 2 (#1–#12); `QA.md` § Distributed Sharding (CAP-002) added with 24-step manual test plan; intent-map + section-index entries added. | #3 |
| DIF-012 | Multi-environment support (staging vs. production). New `environments` table per project (migration `024_environments.sql`) keyed on `(projectId, name)` with `baseUrl` + AES-encrypted `credentials` JSON; `runs.environmentId` column added for audit trail. New `environmentRepo` with `create / listByProject / listByProjectIds / getById / update / remove` (credentials JSON-round-tripped through `JSON.stringify`/`JSON.parse`, mirroring projectRepo so better-sqlite3 doesn't reject the bound object). Project-scoped CRUD endpoints `GET / POST / PATCH / DELETE /api/v1/projects/:id/environments[/:environmentId]` (`admin` on mutations, `qa_lead` on read) registered in `backend/src/middleware/permissions.json`; `baseUrl` SSRF-validated on both POST and PATCH via the existing `utils/ssrfGuard.js` two-layer guard. **Every** project-scoped execution path — `/run`, `/crawl`, `/tests/generate`, `/record`, the CI/webhook `/trigger`, the Vercel/Netlify webhook crawls, and the BullMQ worker — accepts an optional `environmentId` that overrides BOTH `project.url` AND `project.credentials` **for that run only**; the project row is never mutated. The override flows through a single shared helper `backend/src/utils/envScope.js` `envScopedProject(project, environment, { previewUrl })` (replaces four copy-pasted local helpers); `previewUrl` (deploy-preview webhook path) takes precedence over `environment.baseUrl`, and `canonicalUrl` is stamped so the AUTO-015 baseline guard treats env-scoped crawls as preview-style and doesn't overwrite production fingerprints. `environment.credentials` lands in the helper already AES-encrypted (env repo only `JSON.parse`s on read, no decryption) and is assigned verbatim — re-encrypting would double-encrypt and silently break login. Envs without their own credentials transparently inherit the project's auth. Run-scope validation: cross-project / unknown `environmentId` → `400 invalid environmentId`, gate runs BEFORE the no-approved-tests check on `/run` so a bogus envId fails fast with the right error message. API responses strip plaintext passwords via new `sanitiseEnvCredentialsForClient` in `backend/src/utils/projectSanitiser.js` — even POST/PATCH echoes only `{ username, _hasAuth: true }` (REVIEW.md "no plaintext passwords in API responses"). PATCH-without-credentials-key preserves stored secret; blank-password merge falls back to existing stored value so editing name/baseUrl alone never wipes the secret; explicit `credentials: null` clears it. Frontend ships `api.getProjectEnvironments` / `createProjectEnvironment` / `updateProjectEnvironment` / `deleteProjectEnvironment`, a new `EnvironmentsTab` panel on `ProjectDetail.jsx` (CRUD form + table, role-gated via `canEdit={userHasRole(authUser, "admin")}` — qa_lead viewers see the table without mutation buttons, scoped `.pd-env-*` styles in `frontend/src/styles/pages/project-detail.css`), environment dropdowns on `RunRegressionModal.jsx` / `RecorderModal.jsx` / `TestLab.jsx` (only render when project has ≥1 env; selection resets cleanly on project change so a stale envId never leaks into the payload; viewer-role 403s on env-list are swallowed so the modal still works for users below qa_lead), and a new "Environments" panel on `Dashboard.jsx` showing per-environment pass rate, run counts, color-coded thresholds, and clickable last-green-run links (scoped `.dash-env-*` styles in `frontend/src/styles/pages/dashboard.css`). Dashboard backend gains an `environmentPassRates` aggregation in `backend/src/routes/dashboard.js` that buckets completed test runs by `(projectId, environmentId)` over a **90-day window** (`ENV_AGGREGATION_WINDOW_DAYS` constant, surfaced as `windowDays` on each row so the UI can label the time range), with a synthetic `default` bucket per project; batched via `environmentRepo.listByProjectIds()` in a single SQL round-trip (replaces the prior per-project N+1 loop). Payload is `null` when no envs exist (zero-regression for workspaces that haven't adopted the feature). Backend integration coverage in `backend/tests/environments.test.js` (registered in `backend/tests/run-tests.js`) covers CRUD round-trip, AES round-trip of `credentials` (parses JSON-stringified column before `decryptCredentials`), PATCH-without-credentials preserves stored secret, explicit `credentials: null` clears it, cross-workspace ACL on read and PATCH (returns 404, not 403, to avoid leaking existence), cross-project envId rejection on `/run` / `/generate` / `/record`, invalid envId rejection BEFORE the no-tests check, zero-regression no-envId path, project.url invariant across every mutation. E2E coverage in `tests/e2e/specs/environments-ui.spec.mjs` (Tier-2, `RUN_UI_E2E=true`) covers the Environments tab CRUD round-trip and the RunRegressionModal dropdown via `page.route()` mock. Satisfies the PROC-001 no-orphan-routes invariant: every new backend route has a real frontend consumer in this PR. `docs/api/projects.md` documents the endpoint surface, run-payload shape, and run-scoped credential override; `QA.md` § Environments (DIF-012) added with full manual test plan covering EnvironmentsTab CRUD, TestLab + RecorderModal dropdowns, Dashboard panel, API happy-path, and negative/edge cases (viewer 403, admin-only mutations, cross-project envId rejection, baseUrl SSRF rejection on RFC1918/localhost). `docs/changelog.md` updated under `### Added`. | #2 |
| CAP-001 | Data-driven test fixtures. New `test_fixtures(testId, version, format, rows, createdAt)` table keyed on `(testId, version)` (migration `023_test_fixtures.sql`); `format` constrained to `'csv' | 'json'` via single-quoted CHECK literal so the migration parses on both SQLite and Postgres. Per-project iteration cap added as `projects.iterationCap INTEGER` in the same migration (default 10, hard server ceiling 100 — `clampIterationCap` enforces `[1, 100]` regardless of source). New `testFixtureRepo.upsertFixture / getFixture / listFixtures`. `POST /api/v1/tests/:testId/fixtures` (qa_lead+, format allowlist) + `GET …/fixtures` (anyAuthenticatedMember, newest version first) registered in `permissions.json`. Single-field PATCH bypass on `/api/v1/projects/:id` for `iterationCap` mirrors the existing `autoApproveThreshold` shape. `executeTestIterations` in `backend/src/runner/executeTest.js` substitutes `{{column}}` placeholders per row; `testRunner.js` resolves fixtures keyed to the test's current `codeVersion` (zero-regression for fixture-less tests), surfaces every iteration to the run aggregator exactly once after retries resolve (fixing the double-count bug where `processResult` inside the retry callback corrupted `run.passed` / `run.failed`), and emits a visible `↻ Skipping retry for <test>` log line when a data-driven test fails (data-driven tests don't retry — would re-execute every row). CSV parser is RFC 4180-aware (quoted fields, embedded commas / CRLF, escaped quotes). Frontend ships `api.uploadTestFixture` + `api.getTestFixtures`; new `TestFixturePanel.jsx` mounted on `TestDetail.jsx` (CSV / JSON textarea, optional iteration-cap override, history table with active-version badge, `window.confirm` prompt before overwriting an existing fixture at the same codeVersion). `IterationCapPanel` lives under the new **Iterations** inner tab on `ProjectQualityCard` (Automation → Quality Gates). `StepResultsView.jsx` renders an `iteration #N` badge with the substituted row JSON as the tooltip. New `backend/tests/fixture-iteration.test.js` covers the 5-row → 5-results acceptance criterion, fixture-less zero-regression path, failed-iteration row attribution, cap clamp boundary cases, and CSV-parser quoting edge cases. `backend/tests/test-fixtures-routes.test.js` provides HTTP coverage (format allowlist, empty-rows rejection, cap override + truncation, version mirroring of `test.codeVersion`, 404 unknown testId, cross-workspace ACL). `tests/e2e/specs/test-fixtures-ui.spec.mjs` covers the UI (CSV upload round-trip, RunDetail iteration badges via `page.route()` mock, Iterations panel save round-trip). `docs/api/projects.md` + `docs/changelog.md` updated; `QA.md` gained a new "Data-driven test fixtures (CAP-001)" section + intent-map / section-index entries; `tests/e2e/COVERAGE.md` flipped the CAP-001 row to ✅. | #1 |
| AUTO-004 | Test impact analysis (git diff / GitHub PR files). New `backend/src/pipeline/impactAnalysis.js` exports `computeImpactedTests({ tests, changedFiles, changedPages, routeMap })` + `routePrefixesForChangedFiles()` — pure helpers deriving URL route prefixes from file paths via heuristic (`src/app/pages/routes` anchor + kebab-case tokenisation + non-route-folder filtering) with optional `routeMap` override for monorepos / shared component folders. `backend/src/integrations/githubChecks.js` gains `getChangedFilesForPr({ repo, prNumber, installationId })` paginating the PR files API (max 10×100) reusing the TTL-cached installation-token + bounded-retry path. `backend/src/routes/trigger.js` resolves `changedFiles` (body override first, then GitHub fetch when `installationId` is configured), runs `computeImpactedTests(...)`, filters the dispatched queue to impacted tests only, pre-seeds non-impacted approved tests as `{ status: "skipped", skipReason: "skipped_no_impact" }` so every approved test still has a resolution on the run (never silently dropped). `backend/src/pipeline/riskScorer.js` accepts `changedFiles[]` + `routeMap` and adds a `+10` file-affinity boost composing with AUTO-002's `changedPages[]` boost. **Spec extension (deliberate):** migration `022_run_changed_files.sql` adds two columns rather than the single `changedFiles` listed in NEXT.md — `runs.impactAnalysis` (JSON) persists the resolved `{ impactedTestIds, fallbackReason, routePrefixes }` summary alongside `runs.changedFiles`, which is what `RunDetail.jsx`'s new Impact scope panel renders (impacted-vs-approved badge, fallback reason chip, up to 12 changed-file `<code>` pills + "+N more" overflow). Pass-rate denominator in RunDetail now excludes `skipped_no_impact` (same as `over_budget`). **Adjacent change:** `scoreTestRisk()`'s history filter was broadened from excluding only `skipReason === "over_budget"` skips to excluding **all** `status === "skipped"` rows, so the new `skipped_no_impact` skip kind receives the same "not an execution outcome" treatment — a previously skipped test no longer gets a near-maximum risk score on the next run regardless of skip reason. GitHub PR-files fetch failures swallow + log + fall back to the full suite (`fallbackReason: "github_fetch_failed"`), mirroring INT-002's swallow-and-log contract. New `backend/tests/impact-analysis.test.js` (registered in `run-tests.js`) covers file→URL routing, empty-diff fallback, unknown-paths `skipped_no_impact`, merge with `changedPages[]`, route-map override, GitHub fetch-failure → full-suite fallback, and file-affinity risk-boost composition. `docs/api/projects.md` documents the new request shape (`changedFiles`, `routeMap`) and `skipped_no_impact` result status; `docs/changelog.md` updated under `## [Unreleased]`. | #18 |
| INT-002b | GitHub integration polish — installation UX + App-level webhooks. New `backend/src/routes/integrations/github.js` exposes admin-gated `GET /install/start/:projectId` + `GET /install/callback` (OAuth-style installation flow) and HMAC-only `POST /app-webhook` (App-level lifecycle events). `backend/src/integrations/githubChecks.js` extended with `signInstallState` / `verifyInstallState` (10-minute one-shot JWT with Redis-backed nonce, in-memory fallback that logs a one-shot warning so operators notice in multi-replica deploys) and `getInstallationRepos(installationId)` (paginated `GET /installation/repositories` reusing the TTL-cached installation-token + bounded-retry path). `githubCheckSettingsRepo` gained `getByInstallationId`, `disableByInstallationId`, `disableByRepo` — wired into the App-webhook dispatch so `installation.deleted` flips every matching project's `enabled=0` and emits `integration.github.disabled` activity per project, `installation_repositories.removed` narrows the disable to `(installationId, repo)` tuples, and `installation.created` / `suspend` / `unsuspend` log-and-no-op (admins re-enable explicitly via Settings UI, never silently re-flipped). `appSetup.js` `_RAW_BODY_PATH_PATTERN` extended to cover `/integrations/github/app-webhook`; `verifyWebhookSignature` exported from `routes/trigger.js` for reuse. `frontend/src/pages/Settings.jsx` IntegrationsTab gained per-project "Install App" button (`api.getGithubInstallStartUrl`) that redirects to GitHub's App-install URL with `state` + `setup_url`; post-install callback redirects to `/settings?tab=integrations&github=installed&projectId=…` and Settings now honours the `?tab=` query param via lazy `useState` init so the redirect lands on the Integrations tab with a success banner. Manual `installationId` / `repo` fields preserved as escape hatches for GHES / DR / re-bind scenarios. Both `TODO(INT-002b):` markers in `frontend/src/pages/Settings.jsx` and `backend/src/routes/trigger.js` removed; `trigger.js` GitHub path also gained an early-ignore guard that ack-200s when `github_check_settings.enabled=0` (closes the silent-stale-401 loop). New `backend/tests/github-install-callback.test.js` (registered in `run-tests.js`) covers state JWT happy-path / tamper / expiry / replay, callback upsert of `github_check_settings`, App-webhook `installation.deleted` disables only matching rows, `installation_repositories.removed` narrows correctly, invalid HMAC returns 401. `docs/api/projects.md` documents the install/callback + App-webhook surface and the multi-replica nonce caveat; `docs/changelog.md` updated under `## [Unreleased]`. | #17 |

*Full completed list → ROADMAP.md § Completed Work*
