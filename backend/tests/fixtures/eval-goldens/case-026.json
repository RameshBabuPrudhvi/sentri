{
  "id": "case-026",
  "category": "modal",
  "description": "Cancel destructive confirm.",
  "url": "https://eval.local/items/42",
  "snapshot": "<button>Delete item</button><div role='dialog' aria-label='Confirm delete' hidden><p>Are you sure?</p><button>Cancel</button><button>Delete</button></div>",
  "expected": "await page.getByRole('button', { name: 'Delete item' }).click();\nawait expect(page.getByRole('dialog', { name: 'Confirm delete' })).toBeVisible();\nawait page.getByRole('button', { name: 'Cancel' }).click();\nawait expect(page.getByRole('dialog', { name: 'Confirm delete' })).toBeHidden();"
}
