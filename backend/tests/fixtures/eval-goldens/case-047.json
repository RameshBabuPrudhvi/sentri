{
  "id": "case-047",
  "category": "assertion-heavy",
  "description": "Toast notification appears and auto-dismisses.",
  "url": "https://eval.local/settings",
  "snapshot": "<button>Save</button><div role='status' aria-live='polite'></div>",
  "expected": "await page.getByRole('button', { name: 'Save' }).click();\nawait expect(page.getByRole('status')).toContainText('Saved');\nawait expect(page.getByRole('status')).toBeHidden({ timeout: 10_000 });"
}
