/**
 * @deprecated — Re-export stub. Import from "../config/testDialsConfig.js" instead.
 */
export {
  STRATEGY_OPTIONS, WORKFLOW_OPTIONS, QUALITY_OPTIONS,
  FORMAT_OPTIONS, LANGUAGES, TEST_COUNT_OPTIONS, DEFAULT_CONFIG,
} from "../config/testDialsConfig.js";
