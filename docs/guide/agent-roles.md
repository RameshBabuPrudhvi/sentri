# Agent roles

Sentri now supports dormant per-workspace agent role configuration for these roles: planner, codegen, critic, selfheal, crawl_classify, scenario_plan, assertion_enhance, state_explorer.

This configuration is stored and validated but is not used by pipeline dispatch yet (AI-005).
